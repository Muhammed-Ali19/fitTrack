"use client";
import React from "react";
import "../uiverse-theme.css";

export default function PatternBackground() {
    return (
        <div
            aria-hidden
            className="fitness-pattern fixed top-0 left-0 w-screen h-screen -z-10"
        />
    );
}
